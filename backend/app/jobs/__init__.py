"""Background job runners (FastAPI BackgroundTasks).

Phase 4 will add the forecast runner that orchestrates the forecasting
and inventory cores, writes forecast_results + reorder_decisions, and
updates forecast_jobs.status.
"""
